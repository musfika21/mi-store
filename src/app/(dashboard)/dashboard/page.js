import UserInfo from "./UserInfo";

export const metadata = {
  title: "Dashboard",
  description: "Dashboard of MI Store",
};

export default function Dashboard() {
  return (
    <div>
      <UserInfo/>
    </div>
  );
}
